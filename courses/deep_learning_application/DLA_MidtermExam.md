# AI agents

HumanEval 和 commitpack 的評估有何不同? 

什麼情況下我們會想用 MCP 來輔助 AI coding agent?

Roo code 幾個不同模式的使用情境?

什麼是 SDD(spec-driven development)?

# ESP32 platform and audio wakeup word detection

RTOS 是什麼東西?

FreeRTOS 和 Linux 有何不同?

ESP32 常用於什麼樣的系統之中?

Queue/Semaphore/Event Group 有什麼不同? 要如何選用?

ESP32 有那些不同的記憶體?

SRAM 和 PSRAM 有什麼不同?

I2C 和 I2S 分別用在那些場景?

如何用 SDA/SCL 發出 I2C 的 START/STOP 訊號?

什麼是 audio spectrum? 把Audio轉換成頻譜，把它做成一張圖片變成分類器去分類的

什麼是 mel spectrum? 頻率的敏感變成不相等的，例如人對低頻不敏感高頻敏感。可以去改變頻率的寬窄

為什麼我們可以用 2D CNN 來做 audio detection?

# Network Compression

為什麼要壓縮網路?

我們要如何將 MLP/CNN/Transformer 轉換成大型的矩陣運算?  
我們要如何將各式網路轉換成適合在不同硬體執行的格式?

什麼是 systolic array?

如何使用 systolic array 計算矩陣乘法?

Google TPU 和 Nvidia 的 GPU 有什麼相同和不同之處?

什麼是 weight pruning? 我們要如何選擇刪除那些權重?

什麼是 structure pruning?

什麼是 channel pruning?

什麼是 filter pruning?

請說明 iteratibe pruning 的步驟和每一個步驟的目的?

什麼是 lottery hypothesis?

什麼是 quantization? 

為什麼我們在選擇  quantization 參數時要使用 KL divergence?

為什麼 BNN 的矩陣乘法會變成 XNOR 運算?

什麼是 knowledge distillation? 它和網路壓縮有什麼關係?

